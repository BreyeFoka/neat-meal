# Database Configuration
DATABASE_URL=your_neon_database_connection_string_here

# JWT Configuration (REQUIRED FOR SECURITY)
JWT_SECRET=your_super_secure_jwt_secret_key_here_min_32_characters
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081,http://localhost:19006

# Example Neon Database URL format:
# DATABASE_URL=postgresql://username:password@hostname/database?sslmode=require

# Security Notes:
# - JWT_SECRET should be at least 32 characters long and cryptographically random
# - Never commit your actual .env file to version control
# - Use different secrets for development and production
# - Consider using environment-specific secrets management in production
